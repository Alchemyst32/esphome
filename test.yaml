esphome:
  name: test-device

esp32:
  board: esp32dev
  framework:
    type: esp-idf

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

logger:

api:

ota:
- platform: esphome
  password: "Alchemyst"
sensor:
  - platform: A0_my_component
    name: "Sensor de prueba"